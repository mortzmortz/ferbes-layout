import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { Box, Flex } from '../../components';
import pkgJson from '../../../package.json';

<Meta title="Introduction/Getting Started" />

<Flex justifyContent="center">
  <h1>Ferbes Layout</h1>
</Flex>
<Flex justifyContent="center" mt="20px">
  <p>v{pkgJson.version}</p>
</Flex>

<Box mt="60px">

# Getting Started

The instructions below cover how to use `Ferbes Layout` within a React application.

### Adding `Ferbes Layout` to an existing app

```bash
npm i ferbes-layout
# Or if you prefer yarn:
yarn add ferbes-layout
```

### Configuring the theme

Refer to the [FerbesProvider documentation](/docs/logic-ferbesprovider--page) to learn more about configuring `Ferbes Layout`.

</Box>
